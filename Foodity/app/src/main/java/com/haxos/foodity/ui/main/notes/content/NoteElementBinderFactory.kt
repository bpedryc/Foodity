package com.haxos.foodity.ui.main.notes.content

import com.haxos.foodity.data.model.NoteElement

class NoteElementBinderFactory {

    /*fun getViewType(noteElement: NoteElement) : Int {
        return 0
    }

    fun getBinder(viewType: Int) : NoteElementBinder {
        when (viewType) {
            0 -> TextNoteElementBinder()
        }
        return TextNoteElementBinder()
    }*/
}
